VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# Firebase Functions secrets (set in Google Cloud / Firebase Secret Manager, not in Vite):
# RESEND_API_KEY
#
# Geocoding uses OpenStreetMap Nominatim (no Maps API key).
# Summaries use Vertex AI Gemini in Cloud Functions (no AI Studio key). Enable Vertex AI API
# and grant the Functions service account roles/aiplatform.user on the project.
